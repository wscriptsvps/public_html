<h1 class="text-3xl font-bold text-purple-800 mb-6">
    <?php echo $ad_to_edit ? 'Editar Anúncio' : 'Criar Novo Anúncio'; ?>
</h1>

<?php if (!empty($errors)): ?>
    <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4">
        <ul><?php foreach ($errors as $error) echo "<li>$error</li>"; ?></ul>
    </div>
<?php endif; ?>

<div class="bg-white p-6 rounded-lg shadow-md">
    <form action="/admin/ads" method="POST">
        <?php if ($ad_to_edit): ?>
            <input type="hidden" name="ad_id" value="<?php echo $ad_to_edit['id']; ?>">
        <?php endif; ?>

        <div class="space-y-6">
            <div>
                <label for="title" class="block text-gray-700 font-bold mb-2">Título do Anúncio</label>
                <input type="text" id="title" name="title" value="<?php echo htmlspecialchars($ad_to_edit['title'] ?? ''); ?>" class="w-full px-3 py-2 border rounded-lg" required>
            </div>
            <div>
                <label for="content_html" class="block text-gray-700 font-bold mb-2">Conteúdo (HTML permitido)</label>
                <textarea id="content_html" name="content_html" rows="6" class="w-full px-3 py-2 border rounded-lg font-mono"><?php echo htmlspecialchars($ad_to_edit['content_html'] ?? ''); ?></textarea>
                <p class="text-xs text-gray-500 mt-1">Pode usar tags HTML como `<img>`, `<a>`, `<b>`, etc.</p>
            </div>
            <div>
                <label for="display_location" class="block text-gray-700 font-bold mb-2">Local de Exibição</label>
                <select id="display_location" name="display_location" class="w-full px-3 py-2 border rounded-lg">
                    <option value="chat_sidebar" <?php echo ($ad_to_edit['display_location'] ?? '') === 'chat_sidebar' ? 'selected' : ''; ?>>Barra Lateral do Chat</option>
                    <option value="homepage" <?php echo ($ad_to_edit['display_location'] ?? '') === 'homepage' ? 'selected' : ''; ?>>Página Inicial</option>
                </select>
            </div>
            <?php if ($ad_to_edit): ?>
            <div>
                <label for="status" class="block text-gray-700 font-bold mb-2">Status</label>
                <select id="status" name="status" class="w-full px-3 py-2 border rounded-lg">
                    <option value="active" <?php echo $ad_to_edit['status'] === 'active' ? 'selected' : ''; ?>>Ativo</option>
                    <option value="inactive" <?php echo $ad_to_edit['status'] === 'inactive' ? 'selected' : ''; ?>>Inativo</option>
                </select>
            </div>
            <?php endif; ?>
        </div>

        <div class="mt-6">
            <button type="submit" name="save_ad" class="bg-orange-500 text-white font-bold py-2 px-6 rounded-lg hover:bg-orange-600">
                Guardar Anúncio
            </button>
            <a href="/admin/ads" class="ml-4 text-gray-600 hover:underline">Cancelar</a>
        </div>
    </form>
</div>

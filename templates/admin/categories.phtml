<div class="flex justify-between items-center mb-6">
    <h1 class="text-3xl font-bold text-purple-800">Gerenciamento de Categorias de Salas</h1>
    <a href="/admin/categories/create" class="bg-orange-500 text-white font-bold py-2 px-4 rounded-lg hover:bg-orange-600">
        + Criar Nova Categoria
    </a>
</div>

<?php if (!empty($success_message)): ?>
    <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mb-4"><?php echo $success_message; ?></div>
<?php endif; ?>

<div class="bg-white p-6 rounded-lg shadow-md">
    <div class="overflow-x-auto">
        <table class="w-full text-sm text-left text-gray-500">
            <thead class="text-xs text-gray-700 uppercase bg-gray-100">
                <tr>
                    <th class="px-6 py-3">Ordem</th>
                    <th class="px-6 py-3">Ícone</th>
                    <th class="px-6 py-3">Nome</th>
                    <th class="px-6 py-3">Descrição</th>
                    <th class="px-6 py-3 text-center">Ações</th>
                </tr>
            </thead>
            <tbody>
                <?php foreach ($categories as $category): ?>
                <tr class="bg-white border-b hover:bg-gray-50">
                    <td class="px-6 py-4"><?php echo $category['display_order']; ?></td>
                    <td class="px-6 py-4"><div class="w-8 h-8"><?php echo $category['icon_svg']; ?></div></td>
                    <td class="px-6 py-4 font-bold"><?php echo htmlspecialchars($category['name']); ?></td>
                    <td class="px-6 py-4"><?php echo htmlspecialchars($category['description']); ?></td>
                    <td class="px-6 py-4 text-center">
                        <a href="/admin/categories/edit/<?php echo $category['id']; ?>" class="font-medium text-blue-600 hover:underline">Editar</a>
                        <form action="/admin/categories" method="POST" class="inline-block ml-4" onsubmit="return confirm('Tem a certeza?');">
                            <input type="hidden" name="category_id" value="<?php echo $category['id']; ?>">
                            <button type="submit" name="delete_category" class="font-medium text-red-600 hover:underline">Apagar</button>
                        </form>
                    </td>
                </tr>
                <?php endforeach; ?>
            </tbody>
        </table>
    </div>
</div>

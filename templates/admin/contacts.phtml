<div class="flex justify-between items-center mb-6">
    <h1 class="text-3xl font-bold text-purple-800">Mensagens de Contato</h1>
</div>

<?php if (!empty($success_message)): ?>
    <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mb-4">
        <?php echo $success_message; ?>
    </div>
<?php endif; ?>

<div class="bg-white p-6 rounded-lg shadow-md">
    <div class="overflow-x-auto">
        <table class="w-full text-sm text-left text-gray-500">
            <thead class="text-xs text-gray-700 uppercase bg-gray-100">
                <tr>
                    <th class="px-6 py-3">De</th>
                    <th class="px-6 py-3">Assunto</th>
                    <th class="px-6 py-3">Data</th>
                    <th class="px-6 py-3">Status</th>
                    <th class="px-6 py-3 text-center">Ação</th>
                </tr>
            </thead>
            <tbody>
                <?php foreach ($contacts as $contact): ?>
                <tr class="bg-white border-b hover:bg-gray-50 <?php echo $contact['status'] === 'new' ? 'font-bold' : ''; ?>">
                    <td class="px-6 py-4"><?php echo htmlspecialchars($contact['name']); ?></td>
                    <td class="px-6 py-4"><?php echo htmlspecialchars($contact['subject']); ?></td>
                    <td class="px-6 py-4"><?php echo date('d/m/Y H:i', strtotime($contact['created_at'])); ?></td>
                    <td class="px-6 py-4"><span class="px-2 py-1 font-semibold rounded-full <?php echo $contact['status'] === 'new' ? 'bg-blue-200 text-blue-800' : ($contact['status'] === 'read' ? 'bg-gray-200 text-gray-700' : 'bg-purple-200 text-purple-700'); ?>"><?php echo ucfirst($contact['status']); ?></span></td>
                    <td class="px-6 py-4 text-center">
                        <a href="/admin/contacts/view/<?php echo $contact['id']; ?>" class="font-medium text-blue-600 hover:underline">Ver Mensagem</a>
                    </td>
                </tr>
                <?php endforeach; ?>
            </tbody>
        </table>
    </div>
</div>
